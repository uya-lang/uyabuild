workspace {
  name = "write-source-tree"
  default = ["//demo:write"]
  strict = true
}

use legacy

legacy.shell "//demo:write" {
  script = ["sh", "build.sh"]
  inputs = ["build.sh", "input.txt"]
  outputs = ["out/result.txt"]
}
