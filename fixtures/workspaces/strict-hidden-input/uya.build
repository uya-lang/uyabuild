workspace {
  name = "strict-hidden-input"
  default = ["//modes:host"]
  strict = true
}

use legacy

legacy.shell "//modes:host" {
  script = ["sh", "scripts/check-hidden.sh", "out/host.txt", "hidden.txt"]
  inputs = ["scripts/check-hidden.sh"]
  outputs = ["out/host.txt"]
  execution_mode = "host"
}
